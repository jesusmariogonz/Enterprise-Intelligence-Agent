# data/structured/

Esta carpeta debe contener:

- `northwind.db` — base de datos Northwind en SQLite (clientes, órdenes, productos, proveedores, empleados), usada como el sistema transaccional ficticio de "EIA Corp".
- `LICENSE_NORTHWIND.txt` — licencia de Northwind (debe conservarse intacta, sin modificaciones).

**Estado actual:** estos archivos NO están presentes en el repositorio todavía. Deben agregarse antes de que el código de RAG/SQL de los capítulos correspondientes pueda ejecutarse contra datos reales.
